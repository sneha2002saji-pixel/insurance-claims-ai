{
  "name": "insurance-claims-ai",
  "private": true,
  "workspaces": ["apps/web", "packages/shared-types"],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web",
    "type-check": "npm run type-check --workspace=apps/web"
  }
}
